# NDI runtime / SDK package
# We do not commit proprietary NDI binaries.
debs/*ndi*.deb
debs/ndi*.deb

# Accidental curl junk (malformed -H flags in repo root)
/Accept:
/Host:
/User-Agent:
/GET
/POST
